import { RoutePlaceholder } from "../_components/RoutePlaceholder";

export default function DashboardPage() {
  return (
    <RoutePlaceholder
      title="Dashboard"
      hint="Operator dashboard with live exam/vehicle status. No data yet."
    />
  );
}
